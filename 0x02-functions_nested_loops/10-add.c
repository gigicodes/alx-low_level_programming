i#include "main.h"
#include <stdio.h>
/**
 * add - function that adds two integers and shows result
 * @x: first inputted parameter
 * @y: second inputted parameter
 *
 * Return: Always 0 (Correct)
 */
int add(int x, int y)
{
return (x + y);
}
